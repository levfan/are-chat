# docker compose 变量（docker compose 会自动读取本文件；shell 环境变量优先级更高）
# 注意：VERSION 需与 deploy/build.sh 产出的镜像 tag 保持一致

# 业务镜像 tag（docker build -t are-chat:$VERSION）
VERSION=1.0.0

# 宿主机对外端口（nginx 入口；容器内固定 80）
APP_PORT=58080
